server:
  port: 8081

spring:
  data:
    mongodb:
      uri: ${SPRING_DATA_MONGODB_URI:mongodb://localhost:27017/finanscepte_user}

security:
  jwt:
    secret: ${JWT_SECRET:fintrack-secret-key-fintrack-secret-key-123}
    expiration-seconds: ${JWT_EXPIRATION_SECONDS:3600}
